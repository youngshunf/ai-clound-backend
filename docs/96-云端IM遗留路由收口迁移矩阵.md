# 96 · 云端 IM 遗留路由收口 · 迁移矩阵（P0-01 基线）

> 目的：冻结当前遗留调用方清单，作为 `P0-01` 之后的删除零容忍边界。  
> 生成时间：2026-07-23。  
> 负责人分支：`feat/doc96-im-route-cutover`。

## A. 业务模块直连 `ws_router` 清单

以下 16 个文件在当前基线存在 `ws_router` 依赖，按 P1-P4 分片切换到 `hasn_im` 端口。  
迁移完成后，应逐条移除对应白名单行。

### 清单（按文件）

1. `app/hasn/api/ws_node.py`（R1-09：协同协议入口）
2. `app/hasn/api/v1/app/contacts.py`（R1-05：联系人 API）
3. `app/hasn/api/v1/app/hasn_agents.py`（R1-05：agent 能力 API）
4. `app/hasn/api/v1/app/hasn_auth_api.py`（R1-05：授权/会话查询）
5. `app/hasn/service/task_scheduler.py`（R1-05：任务侧发起）
6. `app/hasn/service/workspace_notification_subscriber.py`（R1-05：通知订阅）
7. `app/hasn/service/inbound_release.py`（R1-05：同步入站转发）
8. `app/hasn/service/hasn_agents_service.py`（R1-05：agent/owner 在线与关系）
9. `app/hasn/service/hasn_contacts_service.py`（R1-05：联系人状态更新）
10. `app/hasn/service/hasn_message_hub_service.py`（R1-05：消息中心）
11. `app/hasn/service/sync_invalidate_service.py`（R1-04：同步 invalidate）
12. `app/hasn/service/message_router.py`（R1-04：兼容层）
13. `app/hasn/service/ws_delivery_bus.py`（R1-02：routing 中间层）
14. `app/hasn_community/service/community_service.py`（R1-05：社区服务）
15. `app/hasn_community/service/circle_service.py`（R1-05：社群服务）
16. `app/hasn_im/adapters/ws_realtime_gateway.py`（R1-07：临时兼容网关）

## B. 业务模块直连 `message_router` 清单

1. `app/hasn_im/application/local_gateway.py`（R0-02：兼容入口）
2. `app/hasn/service/hasn_conversations_service.py`（R1-05：会话服务）
3. `app/hasn/service/inbound_release.py`（R1-05：入站分发）
4. `app/hasn/service/owner_message_sync_service.py`（R1-05：owner 同步）
5. `app/hasn/api/v1/app/hasn_agents.py`（R1-05：agent 关联系统）
6. `app/hasn_im/tests/test_architecture_guards.py`（测试引用）

## C. 业务模块直连 `ws_delivery_bus` 清单

1. `app/hasn/service/ws_router.py`（R1-02：兼容层）

## D. 直接访问遗留 IM Redis key 触点

1. `app/hasn/service/ws_router.py`（`hasn:node_*`、`hasn:entity_node`、`hasn:offline:*`、`hasn:push:*`）
2. `app/hasn/service/node_scheduler.py`（`hasn:node_conn` 兼容对齐）

## 当前约束动作（P0-01）

- 禁止新增上述以外的 `ws_router`/`message_router`/`ws_delivery_bus` 直接 import。
- 禁止新增除 D/D 项之外的 legacy redis key 文本访问。
